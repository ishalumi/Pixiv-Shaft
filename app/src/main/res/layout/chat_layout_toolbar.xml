<?xml version="1.0" encoding="utf-8"?>
<!-- Classic Shaft toolbar — same shape as fragment_settings.xml:
       - AppCompat Toolbar (not Material AppBarLayout)
       - bg = ?attr/colorPrimary  (brand-tinted top bar, white text)
       - navigationIcon = back arrow, wired by setupToolbar
       - elevation = six_dp (matches the app-wide settings toolbar)
       - fitsSystemWindows = true so the toolbar handles its own status-bar inset

     Keeps id `app_bar_layout` so existing constraints in
     chat_fragment_demo_list.xml that anchor to @id/app_bar_layout don't
     need rewiring. setupToolbar finds it by the same id and treats it as
     a Toolbar — there's no separate AppBarLayout anymore. -->
<androidx.appcompat.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_bar_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:elevation="@dimen/six_dp"
    android:minHeight="?attr/actionBarSize"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:navigationIcon="@drawable/baseline_arrow_back_24"><!--
        background + top status-bar padding are applied at runtime by
        NavExt.setupToolbar:
          - bg = Shaft.getThemeColor()    (brand color per user's AppTheme_IndexX)
          - paddingTop = status bar inset  (fitsSystemWindows used to do this
            but also wrongly inhaled the bottom nav inset under edge-to-edge,
            leaving a tall purple band below the title)
    -->

    <!-- Toolbar children are centered horizontally via layout_gravity. We
         use a vertical LinearLayout (title + subtitle) so the 1v1 view can
         show a peer's @-style subtitle below the name. Subtitle is `gone`
         by default — global / no-subtitle rooms only see the single title. -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:singleLine="true"
            android:textColor="@color/white"
            android:textSize="18sp"
            tools:text="聊天室" />

        <TextView
            android:id="@+id/tv_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:singleLine="true"
            android:textColor="#CFFFFFFF"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="\@melodygardot"
            tools:visibility="visible" />

    </LinearLayout>

</androidx.appcompat.widget.Toolbar>
