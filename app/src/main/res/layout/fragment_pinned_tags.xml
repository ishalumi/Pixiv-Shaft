<?xml version="1.0" encoding="utf-8"?>
<!--
  「侧边栏 → 置顶标签」页布局。
  Toolbar 用 fragment_settings / fragment_webview 同款 5 件套（colorPrimary 底 + 白标题 +
  返回箭头 + AppBarOverlay / PopupOverlay + title=" " 占位）；BaseActivity 开了 EdgeToEdge，
  状态栏 inset 走 runtime `updatePadding(top=BarUtils.getStatusBarHeight())`，不用
  fitsSystemWindows（双重 inset 会让顶部多空一截）。
  list 容器用 FrameLayout 叠一个居中的 empty view，不嵌 SmartRefreshLayout —— 本地 DB 查询
  毫秒级返回，下拉刷新没意义；新条目通过 onResume 时再查一次 DB 自动出现。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/f5_to_deep_dark"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:elevation="@dimen/six_dp"
        android:theme="@style/AppTheme.AppBarOverlay"
        app:popupTheme="@style/AppTheme.PopupOverlay"
        app:title=" "
        app:titleTextColor="@android:color/white"
        app:navigationIcon="@drawable/ic_arrow_back_white_shadow">

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/pinned_tags"
            android:textColor="@color/white"
            android:textSize="18sp" />

    </androidx.appcompat.widget.Toolbar>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false" />

        <LinearLayout
            android:id="@+id/empty_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/icon_empty_state" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:gravity="center"
                android:text="@string/string_48"
                android:textColor="@color/text10" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
