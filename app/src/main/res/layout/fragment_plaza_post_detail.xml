<?xml version="1.0" encoding="utf-8"?>
<!-- 单帖详情。复用 cell_plaza_post 的卡片视觉 (include),保持跟 feed 一致;
     差异:
     - 卡片整体不可点(本身就在详情里)
     - 底部「删除」按钮 (自己的帖子可见)
     - 内容用 ScrollView 包,正文可以很长
     - state_overlay:加载 / not_found 状态遮挡内容 -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurfaceContainerLow"
    android:theme="@style/Theme.Material3.DayNight.NoActionBar">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="@dimen/six_dp"
        android:minHeight="?attr/actionBarSize"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/baseline_arrow_back_24">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:singleLine="true"
            android:text="@string/plaza_post_detail_title"
            android:textColor="@color/white"
            android:textSize="18sp"
            tools:text="帖子" />
    </androidx.appcompat.widget.Toolbar>

    <ScrollView
        android:id="@+id/content_scroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintBottom_toTopOf="@id/bottom_bar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/app_bar_layout">

        <include
            android:id="@+id/post_card"
            layout="@layout/cell_plaza_post" />
    </ScrollView>

    <!-- not_found 提示。覆盖在内容上方,delete 按钮也隐藏。 -->
    <TextView
        android:id="@+id/gone_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/plaza_post_gone"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/content_scroll"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/content_scroll"
        tools:visibility="visible" />

    <!-- 底部条:删除按钮 (仅自己的帖子)。inset 适配 nav bar 在 fragment 里 runtime 加。 -->
    <LinearLayout
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:gravity="end"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="10dp"
        android:paddingEnd="16dp"
        android:paddingBottom="10dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:visibility="visible">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_delete"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/plaza_delete_confirm_yes" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
