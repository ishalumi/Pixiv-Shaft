<?xml version="1.0" encoding="utf-8"?>
<!--
  批量选择页。toolbar 右上挂 menu_bulk_select_v3（全选 / 反选），
  底部 bar 只剩 hint + 确认按钮。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/v3_bg"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/v3_bg"
        app:elevation="0dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/v3_bg"
            app:contentInsetStartWithNavigation="0dp"
            app:navigationIcon="?attr/homeAsUpIndicator"
            app:title="@string/bulk_select_title"
            app:titleTextAppearance="@style/TextAppearance.AppCompat.Title"
            app:titleTextColor="@color/v3_text_1" />

        <TextView
            android:id="@+id/hint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:paddingHorizontal="16dp"
            android:paddingBottom="10dp"
            android:textColor="@color/v3_text_3"
            android:textSize="11sp"
            tools:text="共 32 项 · 已选 32 项 (GIF 自动跳过)" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/grid"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="4dp"
        android:paddingTop="4dp"
        android:paddingBottom="80dp"
        tools:listitem="@layout/cell_bulk_select_v3" />

    <!-- 底部确认 bar：全选 / 反选 已挪到 toolbar 右上，这里只剩 CTA -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_marginVertical="10dp"
        app:cardBackgroundColor="@color/v3_surface_1"
        app:cardCornerRadius="14dp"
        app:cardElevation="0dp">

        <Button
            android:id="@+id/btnConfirm"
            style="?attr/borderlessButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:paddingVertical="12dp"
            android:textColor="@color/v3_blue"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="加入下载队列 (32 项 / 87 张图) →" />
    </androidx.cardview.widget.CardView>
</LinearLayout>
